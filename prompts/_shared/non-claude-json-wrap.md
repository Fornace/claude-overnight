<!-- shared wrap: sandwiches a planner prompt with an explicit "JSON only" directive -->
<!-- applied when the planner model is not a Claude variant (which already obey schema strictly) -->

OUTPUT: single JSON object. No prose. No markdown fences.

{{innerPrompt}}

OUTPUT: single JSON object. No prose. No markdown fences.
